# Movie Project (CLI + SQLite + OMDb + Static Website)

Dieses Projekt ist eine CLI-Filmdatenbank mit:
- SQLite Speicherung (SQLAlchemy)
- optionalem Abruf von Filmdaten über OMDb API (Titel, Jahr, Rating, Poster)
- Generierung einer statischen Website aus einem HTML-Template

## Setup

```bash
pip install -r requirements.txt
